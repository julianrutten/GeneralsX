# August 2026

## 19/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Merged `thesuperhackers/main` (`33a3f8f00`) into `thesuperhackers-sync-08-19-2026`. Only seven commits and four days behind - the 16/08 sync landed three days ago - so this was a small incremental sync, not the large divergence the sync playbook assumes. 31 files, +638/-178.
- Imported: tunnel network exit fix (#3136), retail smudge particle type cleanup (#3162), nuke missile debug radius draw (#3160), UTF-8 transcoding in WWLib (#2528), `KINDOF_NO_SELECT` fix (#3125), scorch diffuse colour fix (#3153), smudge render early-out (#3140).
- One real conflict, `privateExit`/`privateExitInstantly` in both products' `AIUpdate.cpp`. Upstream #3136 and our PR #254 fix the same tunnel-network regression by different means. Took upstream's virtual `ContainModuleInterface::isContained` and deleted our `isCaveContainer`/`isSharedNetworkExitContainer` helpers - not a loss of capability, because `TunnelContain::getContainedItemsList` is already per-controlling-player and `CaveContain`'s is per cave index, so upstream's membership test implies the conditions our helper checked explicitly. Our `WWMath::Sqrt*Origin` edits in the same files are untouched.
- Two of the imports are cross-platform wins as well as bug fixes: #2528 drops `MultiByteToWideChar`/`WideCharToMultiByte` from shared GameSpy code, and #3162 drops an unaligned little-endian type-pun over a `char*` that was used to spot smudge particles - bad on ARM64.
- Kept `PRESERVE_RETAIL_PARTICLES` at upstream's `1`. It is not a CRC switch; it gates the fixup that lets retail particle templates be recognised as smudges at all, so turning it off would delete smudge rendering against retail data.
- #3160 arrived with bare `ceilf`/`cosf`/`sinf`; routed them through `WWMath::Ceil`/`CosTrig`/`SinTrig` per the determinism rules.
- Verified: Linux `linux64-deploy` configures and builds both binaries from `/work`, before and after the merge, plus a clang++ compile of all 20 merged translation units as a partial macOS proxy. Not verified: macOS, and no runtime smoke test at all - headless container, no display, no Vulkan device, no retail assets. Not pushed; read-only credential.
- Found while validating, all pre-existing and not merge damage: `RTS_BUILD_OPTION_DEBUG=ON` does not compile on 64-bit Linux (`Debug.h` uses MSVC `__int64` under `DEBUG_PROFILE`; with profiling off it then fails on `MAKE_DLINK` casting `Object*` to `unsigned int` in `Object.h`), which also means upstream's new `RTS_DEBUG`-guarded debug-draw call site is compile-untested here. And both products lost upstream #2712's null guard in `Object::xfer`, so `draw->setID()` runs on a possibly-null drawable during save/load - left alone as out of window, but it should be restored.

## 19/08/2026
### Agent Base Image Declared by Dockerfile Instead of Compose
- godmode learned `agent.base_dockerfile`, so the Compose stack added earlier today is gone: deleted `resources/dockerbuild/compose.dev.yml`. This repository runs no services - there is no server and no database - and the `builder` service existed only because naming a Compose service used to be the only way to name an agent base image.
- Cut `godmode.yaml` down to `version`, `name` and an `agent:` block with `base_dockerfile` and `workdir`. As a godmode workspace repository it is validated against a stricter schema that refuses `compose.files`, `agent.base_service`, `routes`, `scope`, `shared`, `database`, `templates`, `bootstrap`, `health`, `inject`, `tenancy`, `verify`, `uses` and `logins` by name, and `name` must be lowercase with dashes - `generalsx`, not `GeneralsX`.
- `workdir: /work` is kept deliberately: `scripts/build/linux/docker-*.sh` all discard `build/<preset>` when its `CMakeCache.txt` was not generated with `CMAKE_HOME_DIRECTORY == /work`, so the mount path decides whether an agent's configure output and a script's survive each other.
- `Dockerfile.dev` keeps its package list and pins untouched. What changed is the two things the move actually affects: the build context is now the repository root (unchanged in practice, and `.dockerignore` still trims it to the three vcpkg manifests), and the named volumes are gone. The vcpkg binary cache does not need one - the prewarm is an image layer - but ccache is now container-local, which is documented rather than worked around; moving `CCACHE_DIR` under the `/work` bind mount would make the compiler cache depend on the caller's write access to the checkout.
- Rewrote the godmode-wiring and build-and-verify sections of `docs/WORKDIR/support/DEV_CONTAINER.md` around plain `docker build` / `docker run`, added the headless replay env block the Compose file used to supply, and updated `resources/dockerbuild/README.md`.
- Still not verified: no Docker socket and no `docker` CLI here either, so the image has never been built or run, and the new `godmode.yaml` has never been through `up`.

## 19/08/2026
### Linux Dev Container and godmode Agent Base Image
- Audited the Linux build end to end and wrote it up in `docs/WORKDIR/support/DEV_CONTAINER.md`.
- Added `resources/dockerbuild/Dockerfile.dev` (`generalsx/linux-dev:latest`), a native Linux dev image whose package list comes from `.github/workflows/build-linux.yml` rather than from the drifted `Dockerfile.linux`. Every package carries a comment naming the file that justifies it.
- Corrections relative to `Dockerfile.linux`: pin `ubuntu:24.04` instead of `ubuntu:26.04`, add the missing `libvulkan-dev`, pin CMake 3.31.6 (the real floor is 3.25 from `cmake_minimum_required` plus `CMakePresets.json` schema v6, not the 3.20 that `docs/BUILD/LINUX.md` still claims), and add clang-tidy, gdb, ccache, git-lfs and p7zip-full.
- Switched vcpkg from bind-mounted to baked, pinned to the commit CI checks out. The mounted scheme cannot work off the original developer's machine: it clones unpinned, and `scripts/docker-vcpkg-init.sh` - named by both `DOCKER_WORKFLOW.md` and `resources/dockerbuild/README.md` - does not exist in the repository.
- Image layers are apt, CMake, vcpkg, then a manifest-only `COPY` and a binary-cache prewarm, so a source edit never invalidates a dependency layer. No game source is ever copied in; the checkout is bind-mounted.
- Added `resources/dockerbuild/compose.dev.yml` with a long-lived `builder` service mounting the checkout at `/work` (matching every `scripts/build/linux/docker-*.sh`) and named volumes for the vcpkg binary cache and ccache.
- Added `godmode.yaml` with `agent.base_service: builder` so a dispatched agent gets a real C++ toolchain. Without it the repo is a godmode workspace repo and an agent lands with no gcc, cmake, ninja or vcpkg at all.
- Also noticed: `linux64-testing` is offered by `AGENTS.md`, `build.instructions.md` and the `workflow_dispatch` menus, but there is no such entry in `CMakePresets.json`.
- Not verified: the image has never been built. No Docker socket was available. `DEV_CONTAINER.md` carries the exact build-and-verify sequence and a list of what is least certain.


## 16/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Created sync branch `thesuperhackers-sync-08-16-2026` and merged upstream `thesuperhackers/main`.
- Reconciled audio subsystem: accepted upstream's `AudioRequest` reference-counted `RefCountPtr<DynamicAudioEventRTS>` and destructor improvements to prevent dangling pointers on looping audio; adapted `MiniAudioManager` and `OpenALAudioManager` to maintain strict cross-platform audio parity.
- Reconciled networking subsystem: unified `ConstructBigCommandList` chunk-based splitting with `NetCommandDataChunk` across `NetPacket.h`, `NetPacket.cpp`, and `Connection.cpp`.
- Maintained platform isolation in `GeneralsMD/Code/Main/CMakeLists.txt` and `Core/Libraries/Source/profile/CMakeLists.txt` (guarded Windows-only `core_debug` dependency).
- Preserved custom CI/CD configuration by rejecting upstream `build-toolchain.yml`.
- Reconciled unified `Core/` file movements (`Shell.h`, `ShellHooks.h`, `ShellMenuScheme.h`, `Shell.cpp`, `ShellMenuScheme.cpp`, and heightmap/scorch additions).
- Verified full clean compilation and linking on macOS (`macos-vulkan` preset) producing both `GeneralsMD/GeneralsXZH` and `Generals/GeneralsX` binaries.

## 15/08/2026
### Fix Shared Tunnel and Cave Network Exits
- Fixed passenger exit commands being rejected when a unit entered one tunnel or cave endpoint and was ordered out through another endpoint in the same player's shared network.
- Kept strict rejection for unrelated containers, separate cave networks, and endpoints controlled by another player.
- Applied the shared gameplay fix to Zero Hour and Generals.

## 14/08/2026
### Restore Windows LAN Map Compatibility
- Kept spaces literal when serializing map names so TheSuperHackers Windows builds can resolve official maps such as Twilight Flame.
- Preserved percent decoding for compatibility with existing GeneralsX replays and reserved-character map names.

## 11/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Created sync branch `thesuperhackers-sync-08-10-2026` and merged upstream `thesuperhackers/main`.
- Resolved logic conflicts in `GameLogicDispatch.cpp` (preserving upstream timing checks alongside local performance optimizations) and `Recorder.cpp`.
- Resolved build system conflicts in `CMakeLists.txt` across `Core/GameEngineDevice`, `Generals/`, and `GeneralsMD/` while preserving platform isolation (`if(WIN32)` blocks).
- Reconciled file movements into `Core/`: applied local custom fixes (`shouldPause` HUD behavior, GeneralsX watermark, Cyrillic font support) to the unified `Core/` classes (`InGamePopupMessage.cpp`, `W3DMainMenu.cpp`, `W3DGameFont.cpp`).
- Verified full compilation and linking on macOS (`macos-vulkan` preset) for Zero Hour and Generals base game.
- Executed full Linux Docker configure (`docker-configure-linux.sh linux64-deploy`) and build scripts (`docker-build-linux-zh.sh` and `docker-build-linux-generals.sh`), confirming clean compilation and linking of `GeneralsXZH` (280M) and `GeneralsX` (268M) binaries.

### Fix Multi-Monitor Window Placement and HiDPI Window Sizing
- Fixed the game opening on a secondary monitor at an oversized width on multi-monitor macOS setups.
- Root cause was a unit mismatch: `buildFilteredResolutions()` clamps the resolution list against `SDL3_GetNativeDisplaySize()`, which returns `mode->w * pixel_density` (physical pixels), but `SDL3_ApplyWindowModeForRenderConfig()` passed those values straight to `SDL_SetWindowSize()`, which expects logical points. On a 2x Retina display a 3840x2160 render resolution produced a 7680-physical-pixel-wide window — twice the width of the whole desktop.
- The oversized window then spanned both displays, and because SDL resolves a window's display by its center point, `SDL_GetDisplayForWindow()` reported the adjacent monitor and the window migrated there.
- `SDL3_ApplyWindowModeForRenderConfig()` now derives the pixel-to-point ratio from the window itself (`SDL_GetWindowSizeInPixels` / `SDL_GetWindowSize`) rather than the display's `pixel_density`. The drawable is only scaled when the window carries `SDL_WINDOW_HIGH_PIXEL_DENSITY`, which is set on macOS only, so the ratio collapses to 1.0 on other platforms and non-HiDPI behavior is unchanged.
- `SDL_CreateWindow()` takes no position argument and resolves to `SDL_WINDOWPOS_UNDEFINED`, which SDL maps to display index 0 of its own enumeration order rather than the OS primary display. `SDL3Main.cpp` now pins the initial placement to `SDL_GetPrimaryDisplay()` while the window is still hidden, so there is no visible jump and fullscreen no longer inherits the wrong display.
- Added `SDL3_CenterWindowOnCurrentDisplay()`, called after the window is resized. SDL keeps the top-left anchor across `SDL_SetWindowSize()`, so growing from the 1024x768 bootstrap size to the configured resolution previously pushed the window off the bottom and right of the display. Centering is clamped to `SDL_GetDisplayUsableBounds()` so the window stays clear of the macOS menu bar, and anchors to the bounds' top-left when the window is larger than the usable area, which keeps the title bar reachable.
- Positioning failures are logged rather than swallowed; Wayland rejects programmatic window positioning by design, so this is a warning and not an error.
- Applied to both Generals and Zero Hour.

### Known Remaining Issue
- `DX8Wrapper::Resize_And_Position_Window()` (`dx8wrapper.cpp`) hardcodes position `(0,0)` on SDL platforms where the Win32 path centers on the current monitor's work area, and passes physical pixels to the `SetWindowPos` compat shim, which forwards them to `SDL_SetWindowSize()`. The window-mode pass in `W3DDisplay` corrects both afterwards, so the visible result is right, but the window can briefly flash at the wrong geometry. Fixing it at source touches shared `Core/` code and needs Linux validation before it lands.
